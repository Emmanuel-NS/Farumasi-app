Here's a complete Python script to overhaul `_buildOrdersTab`:

```python
#!/usr/bin/env python3
"""
Complete Data Table Overhaul for pharmacist_dashboard_screen.dart
Replaces the card-based _buildOrdersTab with an enterprise Data Table2 UI
"""

import re
from pathlib import Path

# File path
FILE_PATH = r"c:\Users\PC\Farumasi-app\lib\screens\pharmacist\pharmacist_dashboard_screen.dart"

def add_imports(content: str) -> str:
    """Add data_table_2 import if not present"""
    if "import 'package:data_table_2/data_table_2.dart'" not in content:
        # Add after other imports
        import_line = "import 'package:data_table_2/data_table_2.dart';\n"
        # Find the last import statement
        lines = content.split('\n')
        last_import_idx = 0
        for i, line in enumerate(lines):
            if line.startswith('import '):
                last_import_idx = i
        lines.insert(last_import_idx + 1, import_line.strip())
        content = '\n'.join(lines)
    return content

def add_state_variables(content: str) -> str:
    """Add required state variables for the table"""
    state_var_pattern = r"(final PharmacistService _service = PharmacistService\(\);)"
    
    new_variables = '''final PharmacistService _service = PharmacistService();

  // Orders Table State Variables
  final Set<String> _selectedOrderIds = {};
  int _ordersTablePage = 0;
  static const int _ordersTablePageSize = 10;'''
    
    content = re.sub(state_var_pattern, new_variables, content, count=1)
    return content

def create_new_build_orders_tab() -> str:
    """Create the new Data Table2-based _buildOrdersTab method"""
    return '''Widget _buildOrdersTab() {
    // Start with master list
    List<PrescriptionOrder> list = List.from(_service.orders);

    // 1. Filter by Status/Type (Tabs)
    if (_ordersFilterIndex == 1) {
      list = list.where((o) => o.prescriptionImageUrl != null).toList();
    } else if (_ordersFilterIndex == 2) {
      list = list.where((o) => o.prescriptionImageUrl == null).toList();
    } else if (_ordersFilterIndex == 3) {
      list = list.where((o) => o.status == OrderStatus.cancelled).toList();
    } else if (_ordersFilterIndex == 4) {
      list = list.where((o) => o.status == OrderStatus.delivered).toList();
    } else if (_ordersFilterIndex == 5) {
      list = list
          .where(
            (o) =>
                o.status != OrderStatus.delivered &&
                o.status != OrderStatus.cancelled &&
                o.status != OrderStatus.pendingReview,
          )
          .toList();
    } else if (_ordersFilterIndex == 6) {
      list = list
          .where(
            (o) =>
                o.status == OrderStatus.outForDelivery ||
                o.status == OrderStatus.driverAssigned,
          )
          .toList();
    }

    // 2. Search Filter
    if (_searchQuery.isNotEmpty) {
      final q = _searchQuery.toLowerCase();
      list = list.where((o) {
        final matchesId = o.id.toLowerCase().contains(q);
        final matchesName = o.patientName.toLowerCase().contains(q);
        final matchesMed = o.items.any((i) => i.name.toLowerCase().contains(q));
        final matchesDriver =
            o.assignedDriverName?.toLowerCase().contains(q) ?? false;
        final matchesPharmacy =
            o.assignedPharmacyName?.toLowerCase().contains(q) ?? false;
        return matchesId ||
            matchesName ||
            matchesMed ||
            matchesDriver ||
            matchesPharmacy;
      }).toList();
    }

    // 3. Sorting
    switch (_sortBy) {
      case "Newest":
        list.sort((a, b) => b.date.compareTo(a.date));
        break;
      case "Oldest":
        list.sort((a, b) => a.date.compareTo(b.date));
        break;
      case "Price: High-Low":
        list.sort((a, b) => b.totalPrice.compareTo(a.totalPrice));
        break;
      case "Price: Low-High":
        list.sort((a, b) => a.totalPrice.compareTo(b.totalPrice));
        break;
      case "Status":
        list.sort((a, b) => a.status.index.compareTo(b.status.index));
        break;
    }

    // Calculate Metrics
    final totalValue = list.fold(0.0, (sum, o) => sum + o.totalPrice);
    final pendingCount = list.where((o) => o.status == OrderStatus.paymentPending).length;
    final inProgressCount = list
        .where((o) =>
            o.status != OrderStatus.delivered &&
            o.status != OrderStatus.cancelled &&
            o.status != OrderStatus.pendingReview)
        .length;
    final completedCount = list.where((o) => o.status == OrderStatus.delivered).length;

    // Pagination
    final totalPages = (list.length / _ordersTablePageSize).ceil();
    final startIdx = _ordersTablePage * _ordersTablePageSize;
    final endIdx = (startIdx + _ordersTablePageSize).clamp(0, list.length);
    final paginatedList = list.sublist(startIdx, endIdx);

    return Column(
      children: [
        // Enhanced Search Bar
        Container(
          margin: const EdgeInsets.fromLTRB(24, 0, 24, 16),
          padding: const EdgeInsets.symmetric(horizontal: 16),
          height: 50,
          decoration: BoxDecoration(
            color: Colors.white,
            borderRadius: BorderRadius.circular(12),
            border: Border.all(color: Colors.grey.shade300),
            boxShadow: [
              BoxShadow(
                color: Colors.grey.shade100,
                blurRadius: 4,
                offset: const Offset(0, 2),
              ),
            ],
          ),
          child: Row(
            children: [
              Icon(Icons.search, color: _primaryGreen),
              const SizedBox(width: 12),
              Expanded(
                child: TextField(
                  onChanged: (val) {
                    setState(() {
                      _searchQuery = val;
                      _ordersTablePage = 0; // Reset pagination on search
                    });
                  },
                  decoration: const InputDecoration(
                    hintText: "Search ID, Name, Driver...",
                    border: InputBorder.none,
                    hintStyle: TextStyle(color: Colors.grey),
                  ),
                ),
              ),
              if (_searchQuery.isNotEmpty)
                IconButton(
                  icon: const Icon(Icons.clear, color: Colors.grey),
                  onPressed: () => setState(() => _searchQuery = ""),
                ),
            ],
          ),
        ),

        // Controls Row: Filters & Sort
        Container(
          height: 50,
          padding: const EdgeInsets.symmetric(horizontal: 24),
          child: Row(
            children: [
              Expanded(
                child: ListView(
                  scrollDirection: Axis.horizontal,
                  children: [
                    _buildFilterChip("All", 0),
                    const SizedBox(width: 8),
                    _buildFilterChip("Shipping", 6),
                    const SizedBox(width: 8),
                    _buildFilterChip("In Progress", 5),
                    const SizedBox(width: 8),
                    _buildFilterChip("Completed", 4),
                    const SizedBox(width: 8),
                    _buildFilterChip("Rejected", 3),
                    const SizedBox(width: 8),
                    _buildFilterChip("Prescriptions", 1),
                  ],
                ),
              ),
              const SizedBox(width: 12),
              Container(
                decoration: BoxDecoration(
                  color: Colors.white,
                  borderRadius: BorderRadius.circular(8),
                  border: Border.all(color: Colors.grey.shade300),
                ),
                child: PopupMenuButton<String>(
                  icon: Icon(Icons.sort, color: _primaryGreen),
                  tooltip: "Sort Orders",
                  onSelected: (val) => setState(() => _sortBy = val),
                  itemBuilder: (ctx) =>
                      [
                            "Newest",
                            "Oldest",
                            "Price: High-Low",
                            "Price: Low-High",
                            "Status",
                          ]
                          .map(
                            (s) => PopupMenuItem(
                              value: s,
                              child: Row(
                                children: [
                                  if (_sortBy == s)
                                    Icon(
                                      Icons.check,
                                      size: 16,
                                      color: _primaryGreen,
                                    ),
                                  if (_sortBy == s) const SizedBox(width: 8),
                                  Text(
                                    s,
                                    style: TextStyle(
                                      color: _sortBy == s
                                          ? _primaryGreen
                                          : Colors.black,
                                      fontWeight: _sortBy == s
                                          ? FontWeight.bold
                                          : FontWeight.normal,
                                    ),
                                  ),
                                ],
                              ),
                            ),
                          )
                          .toList(),
                ),
              ),
            ],
          ),
        ),

        // Key Metrics Summary Header
        if (list.isNotEmpty)
          Container(
            margin: const EdgeInsets.fromLTRB(24, 16, 24, 16),
            padding: const EdgeInsets.all(16),
            decoration: BoxDecoration(
              color: _primaryGreen.withOpacity(0.05),
              borderRadius: BorderRadius.circular(12),
              border: Border.all(color: _primaryGreen.withOpacity(0.2)),
            ),
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceAround,
              children: [
                _buildMetricCard("Total Orders", list.length.toString(), Icons.receipt),
                _buildMetricCard(
                  "Total Revenue",
                  "RWF ${(totalValue / 1000).toStringAsFixed(1)}K",
                  Icons.attach_money,
                ),
                _buildMetricCard("Pending", pendingCount.toString(), Icons.hourglass_bottom),
                _buildMetricCard(
                  "In Progress",
                  inProgressCount.toString(),
                  Icons.local_shipping,
                ),
                _buildMetricCard("Completed", completedCount.toString(), Icons.check_circle),
              ],
            ),
          ),

        // Bulk Action Bar (visible when items selected)
        if (_selectedOrderIds.isNotEmpty)
          Container(
            margin: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),
            padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 12),
            decoration: BoxDecoration(
              color: _primaryGreen.withOpacity(0.1),
              border: Border.all(color: _primaryGreen.withOpacity(0.3)),
              borderRadius: BorderRadius.circular(12),
            ),
            child: Row(
              children: [
                Expanded(
                  child: Text(
                    "${_selectedOrderIds.length} order(s) selected",
                    style: TextStyle(
                      fontWeight: FontWeight.bold,
                      color: _primaryGreen,
                    ),
                  ),
                ),
                TextButton.icon(
                  onPressed: () => setState(() => _selectedOrderIds.clear()),
                  icon: const Icon(Icons.close, size: 18),
                  label: const Text("Clear"),
                ),
                const SizedBox(width: 8),
                ElevatedButton.icon(
                  onPressed: () {
                    // Bulk action: Mark as processed, export, etc.
                    ScaffoldMessenger.of(context).showSnackBar(
                      SnackBar(
                        content: Text(
                          "Bulk action on ${_selectedOrderIds.length} orders",
                        ),
                      ),
                    );
                  },
                  icon: const Icon(Icons.done_all, size: 18),
                  label: const Text("Bulk Action"),
                  style: ElevatedButton.styleFrom(
                    backgroundColor: _primaryGreen,
                    foregroundColor: Colors.white,
                  ),
                ),
              ],
            ),
          ),

        // Data Table
        Expanded(
          child: list.isEmpty
              ? Center(
                  child: Column(
                    mainAxisAlignment: MainAxisAlignment.center,
                    children: [
                      Icon(
                        Icons.search_off,
                        size: 80,
                        color: Colors.grey.shade300,
                      ),
                      const SizedBox(height: 16),
                      Text(
                        "No Orders Found matching your filters.",
                        style: TextStyle(color: Colors.grey.shade700),
                      ),
                    ],
                  ),
                )
              : Column(
                  children: [
                    Expanded(
                      child: DataTable2(
                        columnSpacing: 12,
                        horizontalMargin: 16,
                        headingRowColor: MaterialStateProperty.all(
                          _primaryGreen.withOpacity(0.08),
                        ),
                        headingRowHeight: 56,
                        dataRowHeight: 56,
                        isHorizontalScrollBarVisible: false,
                        isVerticalScrollBarVisible: true,
                        columns: [
                          DataColumn2(
                            label: Center(
                              child: Checkbox(
                                value: _selectedOrderIds.length == paginatedList.length &&
                                    paginatedList.isNotEmpty,
                                onChanged: (val) {
                                  setState(() {
                                    if (val == true) {
                                      _selectedOrderIds
                                          .addAll(paginatedList.map((o) => o.id));
                                    } else {
                                      _selectedOrderIds.removeWhere(
                                        (id) => paginatedList
                                            .any((o) => o.id == id),
                                      );
                                    }
                                  });
                                },
                              ),
                            ),
                            fixedWidth: 50,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Order ID",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 120,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Date",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 100,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Customer",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 140,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Items",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 70,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Total Price",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 120,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Status",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 140,
                          ),
                          DataColumn2(
                            label: const Text(
                              "Actions",
                              style: TextStyle(fontWeight: FontWeight.bold),
                            ),
                            fixedWidth: 140,
                          ),
                        ],
                        rows: List<DataRow>.generate(
                          paginatedList.length,
                          (index) {
                            final order = paginatedList[index];
                            final isSelected = _selectedOrderIds.contains(order.id);
                            final statusInfo = _getStatusInfo(order);

                            return DataRow(
                              selected: isSelected,
                              onSelectChanged: (val) {
                                setState(() {
                                  if (val == true) {
                                    _selectedOrderIds.add(order.id);
                                  } else {
                                    _selectedOrderIds.remove(order.id);
                                  }
                                });
                              },
                              color: MaterialStateProperty.resolveWith((states) {
                                if (states.contains(MaterialState.selected)) {
                                  return _primaryGreen.withOpacity(0.1);
                                }
                                return index.isEven
                                    ? Colors.white
                                    : Colors.grey.shade50;
                              }),
                              cells: [
                                DataCell(
                                  Center(
                                    child: Checkbox(
                                      value: isSelected,
                                      onChanged: (val) {
                                        setState(() {
                                          if (val == true) {
                                            _selectedOrderIds.add(order.id);
                                          } else {
                                            _selectedOrderIds.remove(order.id);
                                          }
                                        });
                                      },
                                    ),
                                  ),
                                ),
                                DataCell(
                                  Text(
                                    order.id.split('-').last,
                                    style: TextStyle(
                                      fontWeight: FontWeight.bold,
                                      color: _primaryGreen,
                                    ),
                                  ),
                                ),
                                DataCell(
                                  Text(
                                    "${order.date.day}/${order.date.month}/${order.date.year}",
                                  ),
                                ),
                                DataCell(
                                  Text(order.patientName),
                                ),
                                DataCell(
                                  Container(
                                    padding: const EdgeInsets.symmetric(
                                      horizontal: 8,
                                      vertical: 4,
                                    ),
                                    decoration: BoxDecoration(
                                      color: Colors.blue.shade50,
                                      borderRadius: BorderRadius.circular(6),
                                    ),
                                    child: Text(
                                      order.items.length.toString(),
                                      style: TextStyle(
                                        color: Colors.blue.shade700,
                                        fontWeight: FontWeight.bold,
                                      ),
                                    ),
                                  ),
                                ),
                                DataCell(
                                  Text(
                                    "RWF ${order.totalPrice.toInt()}",
                                    style: const TextStyle(
                                      fontWeight: FontWeight.bold,
                                    ),
                                  ),
                                ),
                                DataCell(
                                  Container(
                                    padding: const EdgeInsets.symmetric(
                                      horizontal: 8,
                                      vertical: 6,
                                    ),
                                    decoration: BoxDecoration(
                                      color: statusInfo['bgColor'],
                                      borderRadius: BorderRadius.circular(6),
                                      border: Border.all(
                                        color: statusInfo['color']
                                            .withOpacity(0.3),
                                      ),
                                    ),
                                    child: Text(
                                      statusInfo['text'],
                                      style: TextStyle(
                                        fontSize: 12,
                                        fontWeight: FontWeight.bold,
                                        color: statusInfo['color'],
                                      ),
                                    ),
                                  ),
                                ),
                                DataCell(
                                  Row(
                                    children: [
                                      Tooltip(
                                        message: "View Details",
                                        child: IconButton(
                                          icon: Icon(
                                            Icons.visibility,
                                            color: _primaryGreen,
                                            size: 18,
                                          ),
                                          onPressed: () {
                                            if (order.status ==
                                                OrderStatus.pendingReview) {
                                              Navigator.push(
                                                context,
                                                MaterialPageRoute(
                                                  builder: (_) =>
                                                      PrescriptionReviewScreen(
                                                        order: order,
                                                      ),
                                                ),
                                              ).then((_) => setState(() {}));
                                            } else {
                                              Navigator.push(
                                                context,
                                                MaterialPageRoute(
                                                  builder: (_) =>
                                                      OrderDetailsScreen(
                                                        order: order,
                                                      ),
                                                ),
                                              );
                                            }
                                          },
                                        ),
                                      ),
                                      Tooltip(
                                        message: "Edit Order",
                                        child: IconButton(
                                          icon: Icon(
                                            Icons.edit,
                                            color: Colors.orange,
                                            size: 18,
                                          ),
                                          onPressed: () {
                                            // Edit action
                                            ScaffoldMessenger.of(context)
                                                .showSnackBar(
                                              SnackBar(
                                                content: Text(
                                                  "Editing order ${order.id}",
                                                ),
                                              ),
                                            );
                                          },
                                        ),
                                      ),
                                    ],
                                  ),
                                ),
                              ],
                            );
                          },
                        ),
                      ),
                    ),
                    // Pagination Controls
                    Container(
                      padding: const EdgeInsets.symmetric(
                        horizontal: 24,
                        vertical: 16,
                      ),
                      decoration: BoxDecoration(
                        border: Border(
                          top: BorderSide(color: Colors.grey.shade200),
                        ),
                      ),
                      child: Row(
                        mainAxisAlignment: MainAxisAlignment.spaceBetween,
                        children: [
                          Text(
                            "Page ${_ordersTablePage + 1} of ${totalPages.toStringAsFixed(0)} | ${list.length} total orders",
                            style: TextStyle(
                              color: Colors.grey.shade700,
                              fontWeight: FontWeight.w500,
                            ),
                          ),
                          Row(
                            children: [
                              ElevatedButton.icon(
                                onPressed: _ordersTablePage > 0
                                    ? () => setState(
                                          () => _ordersTablePage--,
                                        )
                                    : null,
                                icon: const Icon(Icons.chevron_left, size: 18),
                                label: const Text("Previous"),
                              ),
                              const SizedBox(width: 12),
                              ElevatedButton.icon(
                                onPressed: _ordersTablePage < totalPages - 1
                                    ? () => setState(
                                          () => _ordersTablePage++,
                                        )
                                    : null,
                                icon: const Icon(Icons.chevron_right, size: 18),
                                label: const Text("Next"),
                              ),
                            ],
                          ),
                        ],
                      ),
                    ),
                  ],
                ),
        ),
      ],
    );
  }

  // Helper method to build metric cards
  Widget _buildMetricCard(
    String label,
    String value,
    IconData icon,
  ) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        Icon(icon, color: _primaryGreen, size: 24),
        const SizedBox(height: 6),
        Text(
          value,
          style: const TextStyle(
            fontWeight: FontWeight.bold,
            fontSize: 14,
          ),
        ),
        const SizedBox(height: 2),
        Text(
          label,
          style: TextStyle(
            fontSize: 11,
            color: Colors.grey.shade600,
          ),
        ),
      ],
    );
  }

  // Helper method to determine status color and text
  Map<String, dynamic> _getStatusInfo(PrescriptionOrder order) {
    bool isPaymentPending = order.status == OrderStatus.paymentPending;
    bool isShipping = order.status == OrderStatus.outForDelivery ||
        order.status == OrderStatus.driverAssigned;
    bool isRejected = order.status == OrderStatus.cancelled;
    bool isDelivered = order.status == OrderStatus.delivered;

    if (isRejected) {
      return {
        'color': Colors.red,
        'bgColor': Colors.red.shade50,
        'text': 'Rejected',
      };
    } else if (isDelivered) {
      return {
        'color': const Color(0xFF1E9E68),
        'bgColor': const Color(0xFF1E9E68).withOpacity(0.1),
        'text': 'Completed',
      };
    } else if (isPaymentPending) {
      return {
        'color': Colors.orange,
        'bgColor': Colors.orange.shade50,
        'text': 'Awaiting Payment',
      };
    } else if (isShipping) {
      return {
        'color': Colors.blueAccent,
        'bgColor': Colors.blue.shade50,
        'text': order.status == OrderStatus.outForDelivery
            ? 'Out For Delivery'
            : 'Driver Assigned',
      };
    } else if (order.status == OrderStatus.findingPharmacy) {
      return {
        'color': Colors.purple,
        'bgColor': Colors.purple.shade50,
        'text': 'Broadcasting',
      };
    } else {
      return {
        'color': Colors.blue,
        'bgColor': Colors.blue.shade50,
        'text': 'Processing',
      };
    }
  }'''

def replace_method(content: str) -> str:
    """Replace the entire _buildOrdersTab method"""
    # Pattern to match the entire _buildOrdersTab method
    pattern = r'  Widget _buildOrdersTab\(\) \{.*?^  \}'
    
    new_method = create_new_build_orders_tab()
    
    # Use DOTALL and MULTILINE flags
    content = re.sub(pattern, new_method, content, flags=re.DOTALL | re.MULTILINE)
    return content

def main():
    """Main function to apply all transformations"""
    file_path = Path(FILE_PATH)
    
    # Read the file
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()
    
    print("🔍 Reading file...")
    print(f"   File: {file_path}")
    print(f"   Size: {len(content)} characters\n")
    
    # Step 1: Add imports
    print("📦 Step 1: Adding data_table_2 import...")
    content = add_imports(content)
    print("   ✓ Import added\n")
    
    # Step 2: Add state variables
    print("🔧 Step 2: Adding state variables...")
    original_len = len(content)
    content = add_state_variables(content)
    if len(content) > original_len:
        print("   ✓ State variables added\n")
    else:
        print("   ⚠ Note: Check if state variables were added correctly\n")
    
    # Step 3: Replace _buildOrdersTab method
    print("🔄 Step 3: Replacing _buildOrdersTab method...")
    original_len = len(content)
    content = replace_method(content)
    
    if len(content) > original_len + 1000:  # New content should be significantly larger
        print("   ✓ Method replaced with Data Table2 implementation\n")
    else:
        print("   ⚠ Warning: Replacement may not have worked correctly\n")
    
    # Write back to file
    print("💾 Writing updated file...")
    with open(file_path, 'w', encoding='utf-8') as f:
        f.write(content)
    print(f"   ✓ File saved successfully!\n")
    
    print("✅ Complete! The _buildOrdersTab method has been overhauled with:")
    print("   • Enterprise Data Table2 UI")
    print("   • Key Metrics summary (Total Orders, Revenue, Pending, etc)")
    print("   • Columns: Checkbox, Order ID, Date, Customer, Items, Total Price, Status, Actions")
    print("   • Pagination controls (10 items per page)")
    print("   • Bulk action bar for selected orders")
    print("   • Preserved filtering and sorting logic")
    print("   • State variables for selected orders and pagination")
    print("\n⚠️  Remember to run:")
    print("   flutter pub add data_table_2")
    print("   flutter pub get")

if __name__ == "__main__":
    main()
```

**To use this script:**

1. **Save it** to your project root as `overhaul_orders_table.py`
2. **Run it**:
   ```bash
   python overhaul_orders_table.py
   ```
3. **Add the dependency** to `pubspec.yaml`:
   ```yaml
   dependencies:
     data_table_2: ^4.2.0
   ```
4. **Run**:
   ```bash
   flutter pub get
   ```

**Key Features Implemented:**

✅ **Key Metrics Header** — Total Orders, Total Revenue, Pending, In Progress, Completed  
✅ **Data Table2 Columns** — Checkbox, Order ID, Date, Customer, Items, Total Price, Status, Actions  
✅ **Bulk Action Bar** — Appears when rows selected; includes Clear & Bulk Action buttons  
✅ **Pagination** — 10 items per page with Previous/Next controls  
✅ **Filtering & Sorting** — All original filter tabs and sort logic preserved  
✅ **Status Indicators** — Color-coded badges matching original design  
✅ **Row Selection** — Checkboxes with header "select all" functionality  
✅ **Action Buttons** — View Details (eye icon) & Edit (pencil icon) per row  
✅ **State Variables** — `_selectedOrderIds`, `_ordersTablePage`, `_ordersTablePageSize`  
✅ **Helper Methods** — `_buildMetricCard()`, `_getStatusInfo()`  

The script preserves all original filtering logic (By Status, Search, Sort) while completely replacing the card-based UI with a professional, scrollable data table.